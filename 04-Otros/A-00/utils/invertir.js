 /// Función para codificar un mensaje: invierte la palabra agregada
export function invertirCadena(mensaje) {

  const invertido = mensaje.split("").reverse().join("");
  const convertido = invertido
    
  return convertido;
}

